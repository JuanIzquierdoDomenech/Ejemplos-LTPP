/*
What if we wanted to create an interface so that a class could add things?
From this point in code, we don't know what classes will implement this interface

public interface Summable {
  public ? add(? a , ? b)
}
*/